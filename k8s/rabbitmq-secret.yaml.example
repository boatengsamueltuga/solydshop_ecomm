# Copy to rabbitmq-secret.yaml (in this folder) and fill in real values.
# Never commit rabbitmq-secret.yaml — it's in .gitignore.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secrets
type: Opaque
stringData:
  RABBITMQ_DEFAULT_USER: solydshop
  RABBITMQ_DEFAULT_PASS: changeme
